---
import "@bandplate/ui/tokens/theme.css";
import "@bandplate/ui/tokens/components.css";

export interface Props {
  title: string;
  /** Set for pages that must never be indexed or cached by intermediaries (the login-token page). */
  noindex?: boolean;
}

const { title, noindex = false } = Astro.props;
---

<html lang="en" class="bp-no-js">
  <head>
    <meta charset="utf-8" />
    {/* Removed by the first line of script that runs, so CSS can style the
        no-JS case without guessing. `/takes`' filter panel is the one place
        this matters today: its controls live inside a `<dialog>`, which is
        `display: none` until something opens it — with no script to open it,
        the panel has to render as a plain block instead. Inline and before
        the stylesheet, so there is no frame where a JS-capable browser paints
        the fallback. */}
    <script is:inline>
      document.documentElement.classList.remove("bp-no-js");
    </script>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title} — bandplate</title>
    {noindex && <meta name="robots" content="noindex" />}
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Chivo:wght@300;400;600;900&family=Spline+Sans+Mono:wght@400;600&family=Zilla+Slab:wght@500;600;700&display=swap"
    />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
  </head>
  <body class="bg-ground text-ink font-body">
    <a href="#main" class="bp-skip-link">Skip to content</a>
    <slot />
  </body>
</html>

<style is:global>
  * {
    box-sizing: border-box;
  }
  body {
    margin: 0;
  }
</style>
