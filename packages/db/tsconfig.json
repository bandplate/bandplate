{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    // TypeScript 6+ no longer loads every @types package by default.
    "types": ["node"]
  },
  "include": ["src", "seed", "scripts", "drizzle.config.ts"]
}
